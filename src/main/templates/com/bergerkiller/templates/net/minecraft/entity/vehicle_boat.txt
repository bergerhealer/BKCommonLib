package net.minecraft.world.entity.vehicle.boat;

import net.minecraft.world.entity.Entity;
import net.minecraft.network.syncher.EntityDataAccessor;

import com.bergerkiller.bukkit.common.wrappers.DataWatcher;
import com.bergerkiller.bukkit.common.wrappers.DataWatcher.Key;

class Boat extends Entity {
#if version >= 1.17
    private static optional final (DataWatcher.Key<Integer>) EntityDataAccessor<Integer> DATA_WOOD_TYPE:DATA_ID_TYPE;
#elseif version >= 1.14
    private static optional final (DataWatcher.Key<Integer>) EntityDataAccessor<Integer> DATA_WOOD_TYPE:e;
#elseif version >= 1.9
    private static optional final (DataWatcher.Key<Integer>) EntityDataAccessor<Integer> DATA_WOOD_TYPE:d;
#else
    private static optional final (DataWatcher.Key<Integer>) EntityDataAccessor<Integer> DATA_WOOD_TYPE:###;
#endif

    <code>
    public static final Key<com.bergerkiller.bukkit.common.wrappers.BoatWoodType> DATA_WOOD_TYPE = Key.Type.BOAT_WOOD_TYPE.createKey(T.DATA_WOOD_TYPE, -1);
    </code>
}

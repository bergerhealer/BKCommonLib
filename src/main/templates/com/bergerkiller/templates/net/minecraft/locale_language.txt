package net.minecraft.locale;

import com.bergerkiller.generated.net.minecraft.locale.LanguageHandle;

class Language {
#if version >= 1.17
    private static volatile (LanguageHandle) Language INSTANCE:instance;
#elseif version >= 1.16
    private static volatile (LanguageHandle) Language INSTANCE:d;
#else
    private static final (LanguageHandle) Language INSTANCE:c;
#endif

#if version >= 1.18
    public abstract String get:getOrDefault(String s);
#else
    public synchronized String get:a(String s);
#endif
}

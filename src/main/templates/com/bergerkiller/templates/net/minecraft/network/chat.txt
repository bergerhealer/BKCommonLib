package net.minecraft.network.chat;

import com.google.gson.JsonElement;

import net.minecraft.ChatFormatting;

import net.minecraft.network.chat.ComponentSerialization;
import net.minecraft.nbt.StringTag;
import net.minecraft.nbt.CompoundTag;
import net.minecraft.nbt.Tag;

import com.bergerkiller.generated.net.minecraft.nbt.CompoundTagHandle;
import com.bergerkiller.generated.net.minecraft.network.chat.ComponentHandle;

class Component {
    #bootstrap com.bergerkiller.bukkit.common.internal.CommonBootstrap.initServer();

    // Method safely sets a modifier on a Component, making it mutable if required
#if version >= 1.19
    #require net.minecraft.network.chat.Component public abstract Component makeMutableAndSetChatModifier(Style modifier) {
        if (instance instanceof MutableComponent) {
            return ((MutableComponent) instance).withStyle(modifier);
        } else {
            return (Component.literal("")).append(instance).withStyle(modifier);
        }
    }
#elseif version >= 1.18
    #require net.minecraft.network.chat.Component public abstract Component makeMutableAndSetChatModifier(Style modifier) {
        if (instance instanceof MutableComponent) {
            return ((MutableComponent) instance).withStyle(modifier);
        } else {
            return (new ChatComponentText("")).append(instance).withStyle(modifier);
        }
    }
#elseif version >= 1.16
    #require net.minecraft.network.chat.Component public abstract Component makeMutableAndSetChatModifier(Style modifier) {
        if (instance instanceof MutableComponent) {
            return ((MutableComponent) instance).setChatModifier(modifier);
        } else {
            return (new ChatComponentText("")).addSibling(instance).setChatModifier(modifier);
        }
    }
#else
    #require net.minecraft.network.chat.Component public abstract Component makeMutableAndSetChatModifier:setChatModifier(Style modifier);
#endif

#if version >= 1.19
    public String getText() {
        // Macro defined in global.txt
        return instance#getComponentText();
    }
#elseif version >= 1.18
    public abstract String getText:getContents();
#else
    public abstract String getText();
#endif

    public boolean isEmpty() {
        // Macro defined in global.txt
        boolean isContentEmpty = instance#isComponentContentEmpty();

        return isContentEmpty && instance.getSiblings().isEmpty();
    }

#if version >= 1.18
    public (ComponentHandle) Component addSibling((ComponentHandle) Component sibling) {
        if (instance instanceof MutableComponent) {
            return ((MutableComponent) instance).append(sibling);
        } else {
            return instance.copy().append(sibling);
        }
    }
#elseif version >= 1.16
    public (ComponentHandle) Component addSibling((ComponentHandle) Component sibling) {
        if (instance instanceof MutableComponent) {
            return ((MutableComponent) instance).addSibling(sibling);
        } else {
            return instance.mutableCopy().addSibling(sibling);
        }
    }
#else
    public (ComponentHandle) Component addSibling((ComponentHandle) Component sibling);
#endif

    public boolean isMutable() {
#if version >= 1.16
        return instance instanceof MutableComponent;
#else
        return true;
#endif
    }

#select version >=
#case 1.18:    public abstract (ComponentHandle) MutableComponent createCopy:copy();
#case 1.16:    public abstract (ComponentHandle) MutableComponent createCopy:mutableCopy();
#case 1.13.1:  public abstract (ComponentHandle) Component createCopy:h();
#case 1.13:    public abstract (ComponentHandle) Component createCopy:e();
#case else:    public abstract (ComponentHandle) Component createCopy:f();
#endselect

    public (ComponentHandle) Component setClickableURL(String url) {
        Style modifier = instance.getStyle();
#if version >= 1.21.5
        java.net.URI parsedURI = null;
        try {
            parsedURI = new java.net.URI(url);
        } catch (Throwable t) {
            // Not a valid URL at all
        }
        if (parsedURI != null) {
            modifier = modifier.withClickEvent(new ClickEvent$OpenUrl(parsedURI));
        }
#elseif version >= 1.18
        modifier = modifier.withClickEvent(new ClickEvent(ClickEvent$Action.OPEN_URL, url));
#else
        modifier = modifier.setChatClickable(new ClickEvent(ClickEvent$Action.OPEN_URL, url));
#endif
        return instance#makeMutableAndSetChatModifier(modifier);
    }

    public (ComponentHandle) Component setClickableContent(String content) {
#if version >= 1.15
        Style modifier = instance.getStyle();
  #if version >= 1.21.5
        modifier = modifier.withClickEvent(new ClickEvent$CopyToClipboard(content));
  #elseif version >= 1.18
        modifier = modifier.withClickEvent(new ClickEvent(ClickEvent$Action.COPY_TO_CLIPBOARD, content));
  #else
        modifier = modifier.setChatClickable(new ClickEvent(ClickEvent$Action.COPY_TO_CLIPBOARD, content));
  #endif
        return instance#makeMutableAndSetChatModifier(modifier);
#else
        // Feature is since 1.15, do nothing on older versions
        return instance;
#endif
    }

    public (ComponentHandle) Component setClickableSuggestedCommand(String command) {
        Style modifier = instance.getStyle();
#if version >= 1.21.5
        modifier = modifier.withClickEvent(new ClickEvent$SuggestCommand(command));
#elseif version >= 1.18
        modifier = modifier.withClickEvent(new ClickEvent(ClickEvent$Action.SUGGEST_COMMAND, command));
#else
        modifier = modifier.setChatClickable(new ClickEvent(ClickEvent$Action.SUGGEST_COMMAND, command));
#endif
        return instance#makeMutableAndSetChatModifier(modifier);
    }

    public (ComponentHandle) Component setClickableRunCommand(String command) {
        Style modifier = instance.getStyle();
#if version >= 1.21.5
        modifier = modifier.withClickEvent(new ClickEvent$RunCommand(command));
#elseif version >= 1.18
        modifier = modifier.withClickEvent(new ClickEvent(ClickEvent$Action.RUN_COMMAND, command));
#else
        modifier = modifier.setChatClickable(new ClickEvent(ClickEvent$Action.RUN_COMMAND, command));
#endif
        return instance#makeMutableAndSetChatModifier(modifier);
    }

    public (ComponentHandle) Component setHoverText((ComponentHandle) Component hoverText) {
        Style modifier = instance.getStyle();
#if version >= 1.21.5
        modifier = modifier.withHoverEvent(new HoverEvent$ShowText(hoverText));
#elseif version >= 1.18
        modifier = modifier.withHoverEvent(new HoverEvent(HoverEvent$Action.SHOW_TEXT, hoverText));
#else
        modifier = modifier.setChatHoverable(new HoverEvent(HoverEvent$Action.SHOW_TEXT, hoverText));
#endif
        return instance#makeMutableAndSetChatModifier(modifier);
    }

    // ==========================================================================================================
    // ================================== Serialization and de-serialization ====================================
    // ==========================================================================================================

    public static String chatComponentToJson((ComponentHandle) Component chatComponent) {
#if version >= 1.21.6
        // Serialize into JsonElement
        net.minecraft.core.HolderLookup$Provider holderLookup = org.bukkit.craftbukkit.CraftRegistry.getMinecraftRegistry();
        JsonElement jsonElement = (JsonElement) ComponentSerialization.CODEC.encodeStart(
                 holderLookup.createSerializationContext(com.mojang.serialization.JsonOps.INSTANCE),
                 chatComponent
            )
            .result()
            .orElse(null);
        if (jsonElement == null) {
            return "{}";
        }

        // Stringify JsonElement using GSON
        // There is no easily accessible GSON constant that sets disableHtmlEscaping()
        // We call into ByteBufCodecs.lenientJson() as an alternative
        io.netty.buffer.ByteBuf tmpByteBuf = io.netty.buffer.Unpooled.buffer(1024);
        net.minecraft.network.codec.ByteBufCodecs.lenientJson(Integer.MAX_VALUE).encode(tmpByteBuf, jsonElement);
        return net.minecraft.network.Utf8String.read(tmpByteBuf, Integer.MAX_VALUE);

#elseif version >= 1.21.5
        return Component$Serializer.toJson(chatComponent,
                org.bukkit.craftbukkit.CraftRegistry.getMinecraftRegistry());

#elseif version >= 1.20.5
        return Component$Serializer.toJson(chatComponent,
                net.minecraft.server.MinecraftServer.getDefaultRegistryAccess());

#else
        return Component$Serializer.toJson(chatComponent);
#endif
    }

    public static (ComponentHandle) Component jsonToChatComponent(String jsonString) {
#if version >= 1.21.6
        // Decode json string into JsonElement
        JsonElement jsonelement;
        try {
            jsonelement = com.google.gson.JsonParser.parseString(jsonString);
            if (jsonelement == null) {
                return null; // Invalid json string (?)
            }
        } catch (com.google.gson.JsonParseException e) {
            return null; // Invalid json string
        }

        // Deserialize into Component
        net.minecraft.core.HolderLookup$Provider holderLookup = org.bukkit.craftbukkit.CraftRegistry.getMinecraftRegistry();
        return (Component) ComponentSerialization.CODEC.parse(
                holderLookup.createSerializationContext(com.mojang.serialization.JsonOps.INSTANCE),
                jsonelement
            )
            .result()
            .orElse(null);

#else
        try {
  #if version >= 1.21.5
            return Component$Serializer.fromJson(jsonString,
                    org.bukkit.craftbukkit.CraftRegistry.getMinecraftRegistry());
  #elseif version >= 1.20.5
            return Component$Serializer.fromJson(jsonString,
                    net.minecraft.server.MinecraftServer.getDefaultRegistryAccess());
  #else
            return Component$Serializer.fromJson(jsonString);
  #endif
        } catch (com.google.gson.JsonParseException e) {
            return null; // Invalid json string
        }
#endif
    }

    public static (ComponentHandle) Component nbtToChatComponent((com.bergerkiller.bukkit.common.nbt.CommonTag) Tag nbt) {
#if version >= 1.21.5
        // Use the CODEC for this. It'll turn plaintext strings into StringTag, and more complicated
        // values are turned into a NBT structure (component or list at root)
        com.mojang.serialization.DynamicOps dynamicops = net.minecraft.nbt.NbtOps.INSTANCE;
        Component component = (Component) ComponentSerialization.CODEC.parse(dynamicops, nbt)
                    .resultOrPartial(CompoundTagHandle.createPartialErrorLogger(nbt))
                    .orElse(null);
        if (component == null) {
            component = Component.literal("");
        }
        return component;
#else
        // Only StringTag is supported on this version
        if (nbt instanceof StringTag) {
            String jsonString = ((StringTag) nbt).getAsString();
  #if version >= 1.20.5
            return Component$Serializer.fromJson(jsonString,
                    net.minecraft.server.MinecraftServer.getDefaultRegistryAccess());
  #else
            return Component$Serializer.fromJson(jsonString);
  #endif
        } else {
  #if version >= 1.19
            return Component.literal("");
  #else
            return new ChatComponentText("");
  #endif
        }
#endif
    }

    public static (com.bergerkiller.bukkit.common.nbt.CommonTag) Tag chatComponentToNBT((ComponentHandle) Component chatComponent) {
#if version >= 1.21.5
        com.mojang.serialization.DynamicOps dynamicops = net.minecraft.nbt.NbtOps.INSTANCE;
        return (Tag) ComponentSerialization.CODEC.encodeStart(
                 dynamicops, chatComponent).getOrThrow();
#else
        // Serialize to json and return that as a StringTag
  #if version >= 1.20.5
        String jsonString = Component$Serializer.toJson(chatComponent,
                                    net.minecraft.server.MinecraftServer.getDefaultRegistryAccess());
  #else
        String jsonString = Component$Serializer.toJson(chatComponent);
  #endif
  #if version >= 1.15
        return StringTag.valueOf(jsonString);
  #else
        return new StringTag(jsonString);
  #endif
#endif
    }

    public static (ComponentHandle) Component empty() {
#if version >= 1.19
        return Component.literal("");
#else
        return new ChatComponentText("");
#endif
    }

    public static (ComponentHandle) Component newLine() {
#if version >= 1.19
        return Component.literal("\n");
#else
        return new ChatComponentText("\n");
#endif
    }

    // Gets the immutable EMPTY modifier constant, or creates a new mutable empty modifier
    #require net.minecraft.network.chat.Style public static Style emptyFormat() {
#select version >=
#case 1.17:    return Style.EMPTY;
#case 1.16.2:  return Style.a;
#case 1.16:    return Style.b;
#case else:    return new Style();
#endselect
    }

    // Sets an initial chat color for an EMPTY/new modifier
    #require net.minecraft.network.chat.Style public static Style emptyWithColor(ChatFormatting format) {
        Style modifier = #emptyFormat();
#if version >= 1.18
        return modifier.withColor(format);
#elseif version >= 1.16
        return modifier.setColor(format);
#else
        modifier.setColor(format);
        return modifier;
#endif
    }

    // Sets various properties of the Style
    // On MC 1.15.2 where the modifier was mutable, it modifies in the original modifier
#if version >= 1.18
    #require net.minecraft.network.chat.Style public Style setBold:withBold(Boolean bold);
    #require net.minecraft.network.chat.Style public Style setItalic:withItalic(Boolean italic);
    #require net.minecraft.network.chat.Style public Style setStrikethrough:withStrikethrough(Boolean strikethrough);
    #require net.minecraft.network.chat.Style public Style setUnderlined:withUnderlined(Boolean underlined);
    #require net.minecraft.network.chat.Style public Style setObfuscated:withObfuscated(Boolean obfuscated);
#elseif version >= 1.16
    #require net.minecraft.network.chat.Style public Style setBold(Boolean bold);
    #require net.minecraft.network.chat.Style public Style setItalic(Boolean italic);

    // These were added by CraftBukkit and might be missing on some (forge) server types or have broken remapping
    // This stuff is absolutely awful :(

  #if exists net.minecraft.network.chat.Style public Style setStrikethrough(Boolean strikethrough);
    #require net.minecraft.network.chat.Style public Style setStrikethrough(Boolean strikethrough);
  #else
    #require net.minecraft.network.chat.Style private static Style create_modifier:<init>(ChatHexColor color, Boolean bold, Boolean italic, Boolean underlined, Boolean strikethrough, Boolean obfuscated, ClickEvent clickEvent, HoverEvent hoverEvent, String insertion, MinecraftKey font);
    #require net.minecraft.network.chat.Style public Style setStrikethrough(Boolean strikethrough) {
        return #create_modifier(instance.getColor(), instance.isBold(), instance.isItalic(), instance.isUnderlined(), strikethrough, instance.isRandom(), instance.getClickEvent(), instance.getHoverEvent(), instance.getInsertion(), instance.getFont());
    }
  #endif

  #if exists net.minecraft.network.chat.Style public Style setUnderline(Boolean underlined);
    #require net.minecraft.network.chat.Style public Style setUnderlined:setUnderline(Boolean underlined);
  #elseif exists net.minecraft.network.chat.Style public Style setUnderlined(Boolean underlined);
    #require net.minecraft.network.chat.Style public Style setUnderlined(Boolean underlined);
  #else
    #require net.minecraft.network.chat.Style private static Style create_modifier:<init>(ChatHexColor color, Boolean bold, Boolean italic, Boolean underlined, Boolean strikethrough, Boolean obfuscated, ClickEvent clickEvent, HoverEvent hoverEvent, String insertion, MinecraftKey font);
    #require net.minecraft.network.chat.Style public Style setUnderlined(Boolean underlined) {
        return #create_modifier(instance.getColor(), instance.isBold(), instance.isItalic(), underlined, instance.isStrikethrough(), instance.isRandom(), instance.getClickEvent(), instance.getHoverEvent(), instance.getInsertion(), instance.getFont());
    }
  #endif

  #if exists net.minecraft.network.chat.Style public Style setRandom(Boolean obfuscated);
    #require net.minecraft.network.chat.Style public Style setObfuscated:setRandom(Boolean obfuscated);
  #elseif exists net.minecraft.network.chat.Style public Style setObfuscated(Boolean obfuscated);
    #require net.minecraft.network.chat.Style public Style setObfuscated(Boolean obfuscated);
  #else
    #require net.minecraft.network.chat.Style private static Style create_modifier:<init>(ChatHexColor color, Boolean bold, Boolean italic, Boolean underlined, Boolean strikethrough, Boolean obfuscated, ClickEvent clickEvent, HoverEvent hoverEvent, String insertion, MinecraftKey font);
    #require net.minecraft.network.chat.Style public Style setObfuscated(Boolean obfuscated) {
        return #create_modifier(instance.getColor(), instance.isBold(), instance.isItalic(), instance.isUnderlined(), instance.isStrikethrough(), obfuscated, instance.getClickEvent(), instance.getHoverEvent(), instance.getInsertion(), instance.getFont());
    }
  #endif
#else
    // methods modify the original instance. Returns void.
    #require net.minecraft.network.chat.Style public Style setBold(Boolean bold) {
        instance.setBold(bold);
        return instance;
    }
    #require net.minecraft.network.chat.Style public Style setItalic(Boolean italic) {
        instance.setItalic(italic);
        return instance;
    }
    #require net.minecraft.network.chat.Style public Style setStrikethrough(Boolean strikethrough) {
        instance.setStrikethrough(strikethrough);
        return instance;
    }
    #require net.minecraft.network.chat.Style public Style setUnderlined(Boolean underlined) {
        instance.setUnderline(underlined);
        return instance;
    }
    #require net.minecraft.network.chat.Style public Style setObfuscated(Boolean obfuscated) {
        instance.setRandom(obfuscated);
        return instance;
    }
#endif

    public static (ComponentHandle) Component modifiersToComponent(java.util.Collection<org.bukkit.ChatColor> colors) {
        Style modifier = #emptyFormat();
        java.util.Iterator iter = colors.iterator();
        while (iter.hasNext()) {
            org.bukkit.ChatColor color = (org.bukkit.ChatColor) iter.next();
            if (color == org.bukkit.ChatColor.RESET) {
                modifier = #emptyFormat();
            } else if (color == org.bukkit.ChatColor.BOLD) {
                modifier = modifier#setBold(Boolean.TRUE);
            } else if (color == org.bukkit.ChatColor.ITALIC) {
                modifier = modifier#setItalic(Boolean.TRUE);
            } else if (color == org.bukkit.ChatColor.STRIKETHROUGH) {
                modifier = modifier#setStrikethrough(Boolean.TRUE);
            } else if (color == org.bukkit.ChatColor.UNDERLINE) {
                modifier = modifier#setUnderlined(Boolean.TRUE);
            } else if (color == org.bukkit.ChatColor.MAGIC) {
                modifier = modifier#setObfuscated(Boolean.TRUE);
            } else {
#if version >= 1.12
                ChatFormatting format = org.bukkit.craftbukkit.util.CraftChatMessage.getColor(color);
#else
                #require org.bukkit.craftbukkit.util.CraftChatMessage.StringMessage private static final java.util.Map<Character, net.minecraft.server.ChatFormatting> formatMap;
                java.util.Map formats = #formatMap;
                ChatFormatting format = (ChatFormatting) formats.get(Character.valueOf(color.getChar()));
#endif
                modifier = #emptyWithColor(format);
            }
        }

#if version >= 1.19
        return Component.literal("").withStyle(modifier);
#elseif version >= 1.18
        return new ChatComponentText("").withStyle(modifier);
#else
        return new ChatComponentText("").setChatModifier(modifier);
#endif
    }

}

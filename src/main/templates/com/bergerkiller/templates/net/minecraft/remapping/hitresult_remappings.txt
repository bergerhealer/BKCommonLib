import net.minecraft.core.BlockPos;
import net.minecraft.world.phys.Vec3;

#if version >= 1.18
    #remap net.minecraft.world.phys.BlockHitResult public BlockPos getBlockPos();
#elseif version >= 1.14
    #remap net.minecraft.world.phys.BlockHitResult public BlockPos getBlockPos:getBlockPosition();
#elseif version >= 1.13.2 && exists net.minecraft.world.phys.BlockHitResult public net.minecraft.core.BlockPos getBlockPosition();
    #remap net.minecraft.world.phys.BlockHitResult public BlockPos getBlockPos:getBlockPosition();
#else
    #remap net.minecraft.world.phys.BlockHitResult public BlockPos getBlockPos:a();
#endif

#if version >= 1.18
    #remap net.minecraft.world.phys.HitResult public Vec3 getLocation();
#elseif version >= 1.14
    #remap net.minecraft.world.phys.HitResult public Vec3 getLocation:getPos();
#endif
